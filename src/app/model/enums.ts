export const enum Status {
    TODO,
    INPROGRESS,
    DONE
}

export enum ColorTheme {
    DARK = "dark",
    LIGHT = "light"
}

export enum Keyboard {
    ESC = "Escape",
    DEL = "Delete",
    ENT = "Enter",
    TAB = "Tab"
}